import AdminLayout from '@/Layouts/AdminLayout';
import { Head, router, useForm } from '@inertiajs/react';
import { KeyRound, Plus, Search, ShieldCheck, Trash2, UserPlus, X } from 'lucide-react';
import { useMemo, useState } from 'react';

export default function Index({ adminUsers = [] }) {
    const [search, setSearch] = useState('');
    const [formOpen, setFormOpen] = useState(false);
    const { data, setData, post, processing, errors, reset } = useForm({
        name: '', email: '', password: '', password_confirmation: '', role: 'Admin',
    });

    const submit = (event) => {
        event.preventDefault();
        post(route('admin.accounts.store'), { preserveScroll: true, onSuccess: () => { reset(); setFormOpen(false); } });
    };

    const filteredUsers = useMemo(() => {
        const value = search.trim().toLowerCase();
        if (!value) return adminUsers;
        return adminUsers.filter((admin) => [admin.name, admin.email, admin.role].some((field) => field?.toLowerCase().includes(value)));
    }, [adminUsers, search]);

    const closeForm = () => { reset(); setFormOpen(false); };
    const remove = (admin) => {
        if (admin.is_super_admin) return;
        if (!window.confirm(`Delete admin account ${admin.email}?`)) return;
        router.delete(route('admin.accounts.destroy', admin.id), { preserveScroll: true });
    };

    return <AdminLayout activeNavId="accounts" showGlobalSearch>
        <Head title="Admin Accounts" />
        <div className="mb-6 flex flex-col gap-4 sm:mb-8 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-bold text-white sm:text-3xl">Admin Accounts</h1><p className="mt-2 text-sm text-gray-400">Create and manage administrator accounts for the admin portal.</p></div><button type="button" onClick={() => setFormOpen(true)} className="inline-flex min-h-[44px] items-center justify-center gap-2 rounded-md bg-yellow-500 px-4 py-2.5 text-sm font-bold text-black hover:bg-yellow-400"><UserPlus className="h-4 w-4" />Create Admin</button></div>

        {formOpen && <section className="mb-8 rounded-xl border border-neutral-800 bg-[#111111] p-4 sm:p-6"><div className="mb-6 flex items-start justify-between gap-4"><div><h2 className="text-lg font-bold text-yellow-500">Create admin account</h2><p className="mt-1 text-sm text-gray-400">New accounts are created as regular Admin users. Assign tab permissions from Permissions.</p></div><button type="button" onClick={closeForm} className="flex min-h-10 min-w-10 items-center justify-center rounded-md border border-neutral-700 text-gray-400 hover:text-white" aria-label="Close create admin form"><X className="h-4 w-4" /></button></div><form onSubmit={submit} className="grid grid-cols-1 gap-5 md:grid-cols-2"><div><label htmlFor="admin-name" className="mb-2 block text-sm font-medium text-gray-300">Full name</label><input id="admin-name" type="text" value={data.name} onChange={(event) => setData('name', event.target.value)} className="min-h-[44px] w-full rounded-md border border-neutral-800 bg-[#1a1a1a] px-4 py-2.5 text-white outline-none focus:ring-2 focus:ring-yellow-500" placeholder="Jane Doe" />{errors.name && <p className="mt-1 text-sm text-red-400">{errors.name}</p>}</div><div><label htmlFor="admin-email" className="mb-2 block text-sm font-medium text-gray-300">Email</label><input id="admin-email" type="email" value={data.email} onChange={(event) => setData('email', event.target.value)} className="min-h-[44px] w-full rounded-md border border-neutral-800 bg-[#1a1a1a] px-4 py-2.5 text-white outline-none focus:ring-2 focus:ring-yellow-500" placeholder="admin@example.com" />{errors.email && <p className="mt-1 text-sm text-red-400">{errors.email}</p>}</div><div><label htmlFor="admin-password" className="mb-2 block text-sm font-medium text-gray-300">Password</label><input id="admin-password" type="password" value={data.password} onChange={(event) => setData('password', event.target.value)} className="min-h-[44px] w-full rounded-md border border-neutral-800 bg-[#1a1a1a] px-4 py-2.5 text-white outline-none focus:ring-2 focus:ring-yellow-500" placeholder="At least 8 characters" />{errors.password && <p className="mt-1 text-sm text-red-400">{errors.password}</p>}</div><div><label htmlFor="admin-password-confirmation" className="mb-2 block text-sm font-medium text-gray-300">Confirm password</label><input id="admin-password-confirmation" type="password" value={data.password_confirmation} onChange={(event) => setData('password_confirmation', event.target.value)} className="min-h-[44px] w-full rounded-md border border-neutral-800 bg-[#1a1a1a] px-4 py-2.5 text-white outline-none focus:ring-2 focus:ring-yellow-500" placeholder="Repeat password" />{errors.password_confirmation && <p className="mt-1 text-sm text-red-400">{errors.password_confirmation}</p>}</div><div className="md:col-span-2 flex flex-wrap items-center justify-end gap-3 border-t border-neutral-800 pt-5"><button type="button" onClick={closeForm} className="min-h-[44px] rounded-md border border-neutral-700 px-4 text-sm text-gray-300 hover:text-white">Cancel</button><button type="submit" disabled={processing} className="inline-flex min-h-[44px] items-center gap-2 rounded-md bg-yellow-500 px-5 py-2.5 text-sm font-bold text-black hover:bg-yellow-400 disabled:opacity-50"><Plus className="h-4 w-4" />{processing ? 'Creating...' : 'Create account'}</button></div></form></section>}

        <section className="rounded-xl border border-neutral-800 bg-[#111111] p-4 sm:p-6 md:p-8"><div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-lg font-bold text-yellow-500 sm:text-xl">Administrator list</h2><p className="mt-1 text-sm text-gray-400">{filteredUsers.length} of {adminUsers.length} account{adminUsers.length === 1 ? '' : 's'}</p></div><div className="relative w-full sm:w-80"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-gray-500" /><label htmlFor="admin-search" className="sr-only">Search admin accounts</label><input id="admin-search" type="search" value={search} onChange={(event) => setSearch(event.target.value)} className="min-h-[44px] w-full rounded-md border border-neutral-800 bg-[#1a1a1a] pl-9 pr-4 py-2.5 text-white outline-none focus:ring-2 focus:ring-yellow-500" placeholder="Search name, email, or role" /></div></div>{filteredUsers.length === 0 ? <div className="py-14 text-center"><ShieldCheck className="mx-auto mb-4 h-10 w-10 text-gray-600" /><p className="text-sm text-gray-400">{search ? 'No matching admin accounts found.' : 'No admin accounts found.'}</p></div> : <div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-b border-neutral-800 text-xs uppercase tracking-wide text-gray-500"><tr><th className="px-4 py-3">Administrator</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Permissions</th><th className="px-4 py-3">Created</th><th className="px-4 py-3 text-right">Action</th></tr></thead><tbody>{filteredUsers.map((admin) => <tr key={admin.id} className="border-b border-neutral-800/70 last:border-0"><td className="px-4 py-4"><div className="flex items-center gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-yellow-500/10 font-bold text-yellow-400">{admin.name?.charAt(0)?.toUpperCase() || '?'}</span><div className="min-w-0"><p className="truncate font-semibold text-white">{admin.name}</p><p className="truncate text-xs text-gray-500">{admin.email}</p></div></div></td><td className="px-4 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${admin.is_super_admin ? 'bg-yellow-500/10 text-yellow-400' : 'bg-blue-500/10 text-blue-400'}`}>{admin.is_super_admin ? 'Super Admin' : admin.role}</span></td><td className="px-4 py-4 text-gray-400">{admin.is_super_admin ? 'Full access' : `${admin.permissions_count} assigned`}</td><td className="whitespace-nowrap px-4 py-4 text-gray-500">{admin.created_at ? new Date(admin.created_at).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' }) : '—'}</td><td className="px-4 py-4 text-right">{admin.is_super_admin ? <span className="inline-flex items-center gap-1.5 text-xs italic text-gray-500"><KeyRound className="h-3.5 w-3.5" />Protected</span> : <button type="button" onClick={() => remove(admin)} className="inline-flex min-h-10 items-center gap-1.5 rounded-md border border-red-900 px-3 text-sm text-red-400 hover:bg-red-950"><Trash2 className="h-4 w-4" />Delete</button>}</td></tr>)}</tbody></table></div>}</section>
    </AdminLayout>;
}
